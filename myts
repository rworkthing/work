import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ChannelsMainTableComponent } from './channels-main-table.component';

describe('ChannelsMainTableComponent', () => {
  let component: ChannelsMainTableComponent;
  let fixture: ComponentFixture<ChannelsMainTableComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [ChannelsMainTableComponent]
    }).compileComponents();

    fixture = TestBed.createComponent(ChannelsMainTableComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
