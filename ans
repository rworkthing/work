const multiLingualTextInfoList = Object.keys(this.inputLocaleLayoutFormLocale.controls).map((key, index) => {
    return {
      localeCode: this.inputLocaleLayoutFormLocale.get(key)?.value, // From locale form group
      localeText: this.inputLocaleLayoutForm.get(this.useExistingFormCtrlKeys[index])?.value, // From display name form group
      identifier: 280000000072595, // Assuming fixed identifier
      objectType: formValue.type,
      attributeType: formValue.datatype,
      translationText: this.inputLocaleLayoutFormDesc.get(this.useExistingFormCtrlKeysDesc[index])?.value // From description form group
    };
  });
