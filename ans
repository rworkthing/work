const request = {
  metadataType: 'Attribute',
  attributeList: [attribute],
  productTypeList: this.metadataService.productTypeKeys.map((code, index) => ({
    identifier: this.metadataService.productTypeValues[index],
    code: code
  }))
};
