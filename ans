const multiLingualTextInfoList = (this.completeDeltaData || []).map(delta => ({
  localeCode: delta.rowData?.locale || '',
  translationText: delta.rowData?.displayName || '',
  localeText: delta.rowData?.description || ''
}));
