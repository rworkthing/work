this.inputLocaleLayoutForm.valueChanges.subscribe((formValues) => {
  const currentIndex = this.dataWithlocale.length - 1; // Use the latest entry's index
  this.autoSaveInlineEntry(formValues, currentIndex);
});
