const roleCategoryMapped = productTypeRoleTypeCatRelationshipList.length > 0
    ? productTypeRoleTypeCatRelationshipList.map(categoryValRelationship => ({
        id: categoryValRelationship.categoryIdentifier,
        label: "Category",
        value: categoryValRelationship.categoryIdentifier
    }))
    : [];
