here take a reference from the counters-multiselect-table and counters-multiselect-dialog and create new component named conditions-multiselect-table and condition-multiselect-dialog in which the table will just be like the counter table but the columns in it will be conditions and RelationshipType in which relationship type will be a dropdown value with hardcoded values as (Locked, Mandatory, Optional, Proposed) on selection of which it will store the values in the the channels tabe just like we are doing for the counter and this table should appear when the value of attributeForChannels is Conditions
